# Cutting the P-value if \< 0.001

Returns "\< 0.001" if a number is less than 0.001.

## Usage

``` r
cutP(x)
```

## Arguments

- x:

  a single number, which must be between 0 and 1.

## Value

a single character
