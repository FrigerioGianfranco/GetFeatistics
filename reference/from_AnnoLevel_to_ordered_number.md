# Get an numeric vector from a factor vector of annotation levels.

Given a factor vector of annotation levels, it creates a numeric vector.
Useful for ranking.

## Usage

``` r
from_AnnoLevel_to_ordered_number(AnnoLevel_vector)
```

## Arguments

- AnnoLevel_vector:

  a factor vector, containing annotation levels.

## Value

A numeric vector ranking the annotation levels.
