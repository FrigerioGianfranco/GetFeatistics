# All PubChem properties

This function generates a vector with all the PubChem properties. These
ones are retrieved when passing "all" to the argument properties of the
function getChemData.

## Usage

``` r
all_PubChem_properties()
```

## Value

a character vector with all the PubChem properties.
